Select relevant functions for annotation from the bounded candidate list.
Use the candidate metadata and compact pseudocode sketches to judge relevance. Candidate metadata contains each function's exact structured function_ref, name, signature, existing comment, depth, and flags. Each function sketch is keyed by the same function_ref and contains only structural pseudocode; use that mapping when selecting context and when later requesting pseudocode. Do not expect function names, signatures, comments, or instruction addresses to be repeated there. Do not select functions merely because they are nearby in the call graph.
The sketches omit declarations, comments, duplicate lines, and low-value formatting. Treat omitted sections as unknown rather than assuming they are irrelevant. Prefer a focused but slightly broad selection, usually 6-9 functions when the candidate set allows it. Include relevant default-named `FUN_*` functions at deeper call-graph levels when their sketches show real behavior; do not reject a function solely because its name is still a default name or because it is nested. Avoid selecting only the root and its immediate named-looking helpers, but do not select obvious runtime/library helpers just to increase the count.
Return only JSON in this form:
{"selected_functions":[{"function_ref":{"address":{"space":"ram","offset":"hex"}}}]}
Always keep the root function in the selection. Return address-qualified references exactly as provided; never select by a bare function name.
